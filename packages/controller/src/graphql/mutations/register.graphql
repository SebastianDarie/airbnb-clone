mutation Register($email: String!, $password: String!, $confirm: String!) {
  register(
    credentials: { email: $email, password: $password, confirm: $confirm }
  ) {
    ...RegularUserResponse
  }
}
